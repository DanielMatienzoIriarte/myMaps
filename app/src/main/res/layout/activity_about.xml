<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context="com.example.danmat.mymaps.AboutActivity">

    <include
        android:id="@+id/appbar"
        layout="@layout/activity_action_bar">
    </include>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_below="@id/appbar"
        android:layout_marginTop="@dimen/general_margin">

        <ImageView
            android:id="@+id/about_actionBar_imageView"
            android:layout_width="@dimen/about_userMale_dimen"
            android:layout_height="@dimen/about_userMale_dimen"
            android:layout_marginTop="@dimen/about_topMargin"
            android:layout_centerHorizontal="true"
            android:src="@drawable/user_male" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/general_margin"
            android:padding="@dimen/general_margin"
            android:text="@string/about_aboutText"
            android:textSize="@dimen/coverdata_name"
            android:layout_below="@id/about_actionBar_imageView" />

    </RelativeLayout>

</RelativeLayout>
